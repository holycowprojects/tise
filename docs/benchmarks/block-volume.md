# `block_volume` — daily, against a per-topic table

> ### ⚠ Superseded — this report describes a retired target
>
> **`block_volume` was retired as the product target in D92.** The project now builds toward **`visit_engaged`**, and the shipped extension still trains **`return_24h`** — no target is wired in before it is built end to end.
>
> Every number below was really measured and none of it has been withdrawn — it is kept deliberately, because it is the evidence that *justified* retiring the target. It is not a description of what Tise currently does, and no figure here should be quoted as a current result.
>
> See D92 in [`DECISIONS.md`](../../DECISIONS.md).

Generated by `analysis/block_volume.py`. **Do not edit by hand.** Regenerate with:

```
uv run python analysis/block_volume.py
```

Blocks are local days in **India Standard Time**, trailing window 10, minimum
6 prior blocks, strict `>` on raw counts — every parameter fixed in
**D91 before any model was fitted**. Feature set `bs_1`
(10 features, 11 design columns).
Corpora measured **separately and never merged** (D18).

## Verdict

**The bar is not cleared.** On history-edge the subject-clustered interval includes zero, so under D91 the **base-rate table ships** and the learned model does not. That is an outcome with a written plan, not a failure: a card reading "71% — 11 of your last 15 days" is driven by `category_base_rate` alone, and machine learning has to earn its place rather than be assumed into it.

## The three predictions D91 recorded

| # | Prediction | Verdict | What happened |
|---|---|---|---|
| 1 | Model beats `global_base_rate` on all three | **FAILED** | beats it on **nothing** |
| 2 | Model **not** distinguishable from the bar on history-edge | **HELD** | distinguishable on **nothing** |
| 3 | `same_as_last` beats the bar somewhere | **FAILED** | beats it on **nothing** |

Prediction 2 is written so that **holding it means the model fails its own bar**. That is
deliberate: D91 expected the pattern of D80 and D85 to repeat, and recorded the expectation
rather than discovering it afterwards.

## Results

### history-chrome

124 labels, 61 pooled test rows,
base rate 36.3%.

| Model | Brier | Log loss | Skill vs base rate |
|---|---:|---:|---:|
| `global_base_rate` | 0.2320 | 0.6568 | +0.000 |
| `time_of_day` | 0.2320 | 0.6568 | +0.000 |
| `logreg_bs1` **(the model)** | 0.2439 | 0.7629 | -0.051 |
| `category_base_rate` *(the bar)* | 0.2454 | 0.6868 | -0.058 |
| `same_as_last` | 0.3337 | 1.1312 | -0.438 |
| `majority_class` | 0.3607 | 4.9826 | -0.555 |

**The model against the bar**, paired on the same rows. Positive favours the model:

| Unit | Paired Brier difference (`category_base_rate` − model) |
|---|---|
| categories *(D91's rule uses this one)* | +0.0015 [-0.0608, +0.0561] (95%, subjects, n=5) — **includes zero** |
| rows | +0.0015 [-0.0694, +0.0682] (95%, rows, n=61) — **includes zero** |

**`same_as_last` against the bar** — D91's third prediction:

| Unit | Paired Brier difference (`category_base_rate` − `same_as_last`) |
|---|---|
| categories | -0.0883 [-0.2409, -0.0453] (95%, subjects, n=5) — excludes zero |

**Fitted weights** on all labels, standardised columns. In-sample and reported for
direction and magnitude only — the scores above are what the predictions are checked
against:

| Feature | Standardised weight |
|---|---:|
| `lastRatio` | +1.083 |
| `prevAbove` | +0.597 |
| `daysSinceAbove__missing` | -0.384 |
| `topicShare10` | +0.257 |
| `streakAbove` | -0.254 |
| `medianLevel` | -0.216 |
| `activeRate10` | -0.176 |
| `dayOfWeekSin` | +0.103 |
| `daysSinceAbove` | -0.031 |
| `totalRatio` | -0.024 |
| `dayOfWeekCos` | -0.015 |

### history-edge

196 labels, 90 pooled test rows,
base rate 39.3%.

| Model | Brier | Log loss | Skill vs base rate |
|---|---:|---:|---:|
| `category_base_rate` *(the bar)* | 0.2479 | 0.6888 | +0.020 |
| `global_base_rate` | 0.2529 | 0.6996 | +0.000 |
| `time_of_day` | 0.2529 | 0.6996 | +0.000 |
| `logreg_bs1` **(the model)** | 0.2607 | 0.7172 | -0.031 |
| `same_as_last` | 0.4140 | 1.3972 | -0.637 |
| `majority_class` | 0.4444 | 6.1402 | -0.757 |

**The model against the bar**, paired on the same rows. Positive favours the model:

| Unit | Paired Brier difference (`category_base_rate` − model) |
|---|---|
| categories *(D91's rule uses this one)* | -0.0128 [-0.0327, +0.0024] (95%, subjects, n=5) — **includes zero** |
| rows | -0.0128 [-0.0294, +0.0032] (95%, rows, n=90) — **includes zero** |

**`same_as_last` against the bar** — D91's third prediction:

| Unit | Paired Brier difference (`category_base_rate` − `same_as_last`) |
|---|---|
| categories | -0.1661 [-0.2296, +0.0006] (95%, subjects, n=5) — **includes zero** |

**Fitted weights** on all labels, standardised columns. In-sample and reported for
direction and magnitude only — the scores above are what the predictions are checked
against:

| Feature | Standardised weight |
|---|---:|
| `medianLevel` | +0.563 |
| `topicShare10` | -0.267 |
| `activeRate10` | -0.243 |
| `dayOfWeekSin` | +0.226 |
| `daysSinceAbove` | -0.194 |
| `lastRatio` | +0.175 |
| `totalRatio` | -0.115 |
| `streakAbove` | +0.065 |
| `prevAbove` | -0.041 |
| `dayOfWeekCos` | +0.026 |
| `daysSinceAbove__missing` | +0.000 |

### history-firefox

83 labels, 35 pooled test rows,
base rate 47.0%.

| Model | Brier | Log loss | Skill vs base rate |
|---|---:|---:|---:|
| `time_of_day` | 0.2593 | 0.7118 | +0.000 |
| `global_base_rate` | 0.2593 | 0.7118 | +0.000 |
| `category_base_rate` *(the bar)* | 0.2641 | 0.7220 | -0.019 |
| `logreg_bs1` **(the model)** | 0.3322 | 0.9140 | -0.281 |
| `majority_class` | 0.5429 | 7.4998 | -1.094 |
| `same_as_last` | 0.5939 | 1.9862 | -1.291 |

**The model against the bar**, paired on the same rows. Positive favours the model:

| Unit | Paired Brier difference (`category_base_rate` − model) |
|---|---|
| categories *(D91's rule uses this one)* | -0.0681 [-0.0852, -0.0130] (95%, subjects, n=3) — excludes zero |
| rows | -0.0681 [-0.1444, +0.0056] (95%, rows, n=35) — **includes zero** |

**`same_as_last` against the bar** — D91's third prediction:

| Unit | Paired Brier difference (`category_base_rate` − `same_as_last`) |
|---|---|
| categories | -0.3298 [-0.4043, +0.1493] (95%, subjects, n=3) — **includes zero** |

**Fitted weights** on all labels, standardised columns. In-sample and reported for
direction and magnitude only — the scores above are what the predictions are checked
against:

| Feature | Standardised weight |
|---|---:|
| `totalRatio` | +0.879 |
| `medianLevel` | -0.775 |
| `daysSinceAbove__missing` | -0.767 |
| `streakAbove` | -0.475 |
| `lastRatio` | +0.448 |
| `daysSinceAbove` | +0.235 |
| `prevAbove` | +0.213 |
| `activeRate10` | +0.174 |
| `dayOfWeekSin` | +0.138 |
| `topicShare10` | -0.118 |
| `dayOfWeekCos` | -0.008 |


## Limitations

- **One person's browsing**, three browsers, measured separately. As everywhere here.
- **Blocks come from imported history**, reconstructed through the redirect heuristic rather
  than observed. The import-versus-live offset has never been measured (D88).
- **The base rate is not 50%** — 36-47% at daily granularity (D90). D88's claim that a median
  split is balanced by construction is false on real data. What daily buys is a target that
  is not *saturated*, which is weaker.
- **Volume trends contaminate the target** (D89). A ten-block window spans ten days here
  rather than seventy, which shrinks the effect but does not remove it.
- **The fitted weights are in-sample.** They say which direction a feature pushes, not
  whether it helps out of sample.

# Attention — is dwell predictable?

Generated by `analysis/attention.py`. **Do not edit by hand.** Regenerate with:

```
uv run python analysis/attention.py
```

One label per visit: **did you stay longer than your recent median for this category?**
Trailing window 20 visits, minimum 10 prior.
Feature set `as_1`, **`full` compat class** — it needs dwell, which the
`chrome.history` API cannot supply. Firefox records no duration and is skipped (D18: corpora
are never merged).

## This is a scouting measurement, not an adoption test

D91 fixed the rule: a performance bar is pre-registered **before** a model is fitted, or it
is not a bar. No bar exists for this target. **Nothing on this page can adopt anything** —
it says whether the direction earns a pre-registration, and nothing else.

## What it found

- **10,502 labels** across 2 corpora — against `block_volume`'s 403 and
  `return_24h`'s 998. The unit is a visit, which is the finest the data contains.
- Model beats the bar (`category_base_rate`) on: `history-chrome`, `history-edge`
- Model beats the floor (`global_base_rate`) on: `history-chrome`, `history-edge`
- Separated from the bar by a subject-clustered interval on: `history-chrome`, `history-edge`

## Results

### history-chrome

5,129 events, 5,070 with a recorded duration.
**4,935 labels**, 2,224 pooled test
rows, base rate 50.3%.

| Model | Brier | Log loss | Skill |
|---|---:|---:|---:|
| `logreg_as1` **(the model)** | 0.2488 | 0.6905 | +0.005 |
| `global_base_rate` | 0.2500 | 0.6931 | +0.000 |
| `time_of_day` | 0.2501 | 0.6933 | -0.000 |
| `category_base_rate` *(the bar)* | 0.2508 | 0.6948 | -0.003 |
| `same_as_last` | 0.4549 | 1.5310 | -0.820 |
| `majority_class` | 0.4955 | 6.8456 | -0.982 |

**Model against the bar**, paired, subject-clustered. Positive favours the model:

+0.0020 [+0.0012, +0.0319] (95%, subjects, n=9) — excludes zero

**Model against the constant** (`global_base_rate`) — the comparison that carries weight here,
since a per-category median split makes the nominal bar ~50% for every category:

+0.0012 [-0.0051, +0.0191] (95%, subjects, n=9) — **includes zero**

**Fitted weights**, in-sample, standardised columns:

| Feature | Standardised weight |
|---|---:|
| `arrivedLink` | -0.235 |
| `dwellLevel` | -0.197 |
| `lastDwellRatio` | +0.152 |
| `sessionPosition` | -0.116 |
| `isSessionStart` | -0.113 |
| `arrivedTyped` | -0.074 |
| `sameAsPrevious` | +0.050 |
| `isWeekend` | -0.040 |
| `meanRatio` | -0.036 |
| `hourCos` | +0.027 |
| `arrivedBookmark` | -0.017 |
| `hourSin` | -0.011 |

### history-edge

5,755 events, 5,709 with a recorded duration.
**5,567 labels**, 2,805 pooled test
rows, base rate 50.0%.

| Model | Brier | Log loss | Skill |
|---|---:|---:|---:|
| `logreg_as1` **(the model)** | 0.2446 | 0.6820 | +0.022 |
| `global_base_rate` | 0.2501 | 0.6933 | +0.000 |
| `time_of_day` | 0.2504 | 0.6940 | -0.001 |
| `category_base_rate` *(the bar)* | 0.2511 | 0.6954 | -0.004 |
| `same_as_last` | 0.4601 | 1.5450 | -0.840 |
| `majority_class` | 0.5159 | 7.1269 | -1.063 |

**Model against the bar**, paired, subject-clustered. Positive favours the model:

+0.0065 [+0.0048, +0.0201] (95%, subjects, n=11) — excludes zero

**Model against the constant** (`global_base_rate`) — the comparison that carries weight here,
since a per-category median split makes the nominal bar ~50% for every category:

+0.0055 [-0.0001, +0.0097] (95%, subjects, n=11) — **includes zero**

**Fitted weights**, in-sample, standardised columns:

| Feature | Standardised weight |
|---|---:|
| `dwellLevel` | -0.256 |
| `arrivedLink` | -0.212 |
| `arrivedTyped` | -0.141 |
| `sameAsPrevious` | -0.118 |
| `sessionPosition` | -0.088 |
| `arrivedBookmark` | -0.047 |
| `meanRatio` | -0.040 |
| `hourSin` | -0.033 |
| `isWeekend` | -0.014 |
| `isSessionStart` | +0.014 |
| `lastDwellRatio` | +0.012 |
| `hourCos` | -0.005 |


## Limitations

- **One person's browsing**, two browsers. Firefox has no duration column at all.
- **Dwell comes from the history database**, which the extension cannot read. Shipping this
  would need the `tabs` permission — the decision this page exists to inform.
- **`visit_duration` is not attention.** It measures how long a tab held a URL, not how long
  a person looked at it. A tab left open overnight records a long duration and no attention.
  Nothing here corrects for that, and `idle` plus window focus is what would.
- **No path or URL features.** `Visit` drops the URL at the reader boundary and this
  measurement does not reach past it.
- **The model is exploratory**, fitted with no pre-registered bar. Its score is a reason to
  pre-register or to stop, not a result.

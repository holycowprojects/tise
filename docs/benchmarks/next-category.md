# T-C · `next_category` — what comes next?

Generated by `analysis/next_category.py`. **Do not edit by hand.** Regenerate with:

```
uv run python analysis/next_category.py
```

One label per **category change**: the run of visits you just finished ended, and something
different started. Within sessions as well as across their boundaries (D94). Multiclass,
scored on **top-1 accuracy**, top-3 reported alongside.

## The model being tested has existed since T10 and had never been scored

`TransitionTable` ships in both languages and drives the "what's next" surface. Nothing had
ever measured it. That is T-C, and it is the reason this page exists.

## Everything here was fixed in D94, before anything was fitted

- **Label:** every category change, within sessions as well as between them.
- **Metric:** top-1 accuracy. Top-3 is reported and is **not** the bar — reading a verdict
  off it after seeing top-1 would be choosing the metric from the result.
- **Bar:** the always-the-global-mode floor.
- **Cluster unit:** session, with the source category reported side by side.
- **Adoption:** the interval on the accuracy difference against the floor excludes zero in
  the model's favour on `history-edge`.

### The rule was pre-registered. The 33.2% was not the number.

D91 and T19 measured **33.2%** for the always-the-mode rule over a
different label set: one primary category per session, self-transitions permitted. T-C's
labels are category *changes*, so the same rule scores differently here and its value is
**measured on these labels**:

| Corpus | Measured floor | Headline labels |
|---|---:|---:|
| `history-chrome` | 57.3% | 596 |
| `history-edge` | 43.1% | 792 |
| `history-firefox` | 50.0% | 202 |

Fixing the *number* would also have made D94's own adoption rule inapplicable — a paired
bootstrap needs the reference's prediction on every row, and a constant has none.

### A label is a change, and that choice costs something

Consecutive visits in one category collapse into a single run, so `from` and `to` always
differ. That removes the easy, high-volume, highly predictable mass — "you will keep doing
what you are doing" — and leaves the hard half. It also means a session boundary where the
category did not change produces **no** label; each corpus below reports how many.

### `unknown` is excluded from the answer, not from the question (D27)

No surface can show a chip reading *unknown*, and that bucket is the person's own
frequent domains, so it is both unpresentable and unusually predictable. Labels answering
`unknown` are dropped from the headline; labels *conditioned on* it are kept. The
all-labels variant is printed for every corpus.

## Result

**`transition_table` clears its bar on `history-edge`.** +0.0987 [+0.0202, +0.1725] (95%, sessions, n=124) — excludes zero

| Corpus | Labels | `transition_table` | Clears the bar? | Beats `constrained_mode`? |
|---|---:|---:|---|---|
| `history-chrome` | 596 | 58.4% | **not established** | **not established** |
| `history-edge` | 792 | 52.9% | **yes** | **not established** |
| `history-firefox` | 202 | 63.1% | **yes** | **not established** |

Every column is read off a session-clustered interval, not a point estimate. Two models can
differ by four points and support no claim at all, and this project has published that
mistake before (D80).

`bounce_back` predicts the category you were on *before* the one just finished and
reads no counts at all. It is the cheapest thing that could make a fitted table redundant,
and D24 makes baselines mandatory for exactly that reason. It is **not** the bar.

### The bar is wrong before it starts on a third of the rows

A label is a category *change*, so the answer can never be the source. `global_mode` does
not know that and predicts one category regardless; on every row where the source **is** the
global mode it is guaranteed wrong. Per corpus, the share of the bar's predictions that were
impossible by construction:

| Corpus | Bar's impossible predictions | `global_mode` | `constrained_mode` |
|---|---:|---:|---:|
| `history-chrome` | 26.0% | 57.3% | 58.9% |
| `history-edge` | 31.9% | 43.1% | 57.1% |
| `history-firefox` | 27.0% | 50.0% | 65.6% |

`transition_table` never does this — a source's row in the table holds no self-count — so
part of its margin is the floor being handicapped rather than counts being learned.

**`constrained_mode` separates the two.** It is the global mode with the source category
removed: it knows the one thing the label guarantees and nothing else. Whatever the table
beats it by is what the counts are worth on top of that.

**It was added after these numbers existed, and it cannot change the verdict.** D94 named
`global_mode` before anything was fitted and that comparison stands exactly as written. This
one is reported beside it. It was found by asking why the floor was so low, which is a
different act from choosing a bar that flatters a model — but the ordering is the reader's
to judge, so it is stated rather than smoothed over.

## Results

### `history-chrome`

5,129 events, 147 sessions, **794 category
changes** (710 within a session, 84
across a boundary). 62 session boundaries produced no label
because the category did not change. After D27, **596 labels** carry a
presentable answer; 198 answered `unknown`.

**Does not clear its bar** — the session-clustered interval includes zero.

| Model | Top-1 | Top-3 | Rows |
|---|---:|---:|---:|
| `bounce_back` *(a rival, not the bar)* | 64.8% | 75.7% | 358 |
| `constrained_mode` *(a rival, added post-hoc, not the bar)* | 58.9% | 71.5% | 358 |
| `transition_table` **(the model)** | 58.4% | 75.4% | 358 |
| `global_mode` *(the bar)* | 57.3% | 64.5% | 358 |

- **Session-clustered, the pre-registered unit:** +0.0112 [+0.0000, +0.0246] (95%, sessions, n=65) — **includes zero**
- Source-category clustered, D94's side-by-side: +0.0112 [+0.0000, +0.0321] (95%, subjects, n=12) — **includes zero**
- Rows, the optimistic bound: +0.0112 [+0.0028, +0.0223] (95%, rows, n=358) — excludes zero
- The table against `bounce_back` *(a rival)*: -0.0642 [-0.1244, -0.0070] (95%, sessions, n=65) — excludes zero
- `bounce_back` against the bar: +0.0754 [+0.0215, +0.1306] (95%, sessions, n=65) — excludes zero
- The table against `constrained_mode` *(a rival, post-hoc)*:
  -0.0056 [-0.0362, +0.0162] (95%, sessions, n=65) — **includes zero**

**The bar spent 26.0% of its predictions on the source
category**, which a category *change* can never answer. The table did so on 0.0% of rows,
because a source's row holds no self-count. That much of the margin below is the floor
being handicapped rather than the table learning.

Pooled test rows span 65 sessions, median 3
rows each, largest 19%.

**With `unknown` answers kept** (794 labels), for
comparison and not for the verdict: the bar reaches
40.9% and the table
62.7%, session-clustered
+0.2180 [+0.1450, +0.2623] (95%, sessions, n=69) — excludes zero.

#### Folds

| Fold | Train | Test | `global_mode` | `transition_table` | Winner |
|---:|---:|---:|---:|---:|---|
| 0 | 238 | 71 | 56.3% | 57.7% | table |
| 1 | 309 | 71 | 38.0% | 38.0% | tie |
| 2 | 380 | 71 | 66.2% | 66.2% | tie |
| 3 | 451 | 71 | 73.2% | 73.2% | tie |
| 4 | 522 | 74 | 52.7% | 56.8% | table |

#### By source category

| From | Test rows | `global_mode` | `transition_table` | Most common answer |
|---|---:|---:|---:|---|
| `unknown` | 140 | 73.6% | 73.6% | `search` |
| `search` | 93 | 0.0% | 4.3% | `travel` |
| `dev` | 50 | 70.0% | 70.0% | `search` |
| `travel` | 35 | 100.0% | 100.0% | `search` |
| `ai` | 21 | 76.2% | 76.2% | `search` |
| `social` | 7 | — | — | *below the 20-row floor (D26)* |
| `news` | 3 | — | — | *below the 20-row floor (D26)* |
| `video` | 3 | — | — | *below the 20-row floor (D26)* |
| `government` | 2 | — | — | *below the 20-row floor (D26)* |
| `learning` | 2 | — | — | *below the 20-row floor (D26)* |
| `finance` | 1 | — | — | *below the 20-row floor (D26)* |
| `work` | 1 | — | — | *below the 20-row floor (D26)* |

### `history-edge`

5,755 events, 239 sessions, **905 category
changes** (797 within a session, 108
across a boundary). 130 session boundaries produced no label
because the category did not change. After D27, **792 labels** carry a
presentable answer; 113 answered `unknown`.

**Clears its bar.**

| Model | Top-1 | Top-3 | Rows |
|---|---:|---:|---:|
| `bounce_back` *(a rival, not the bar)* | 57.6% | 89.3% | 476 |
| `constrained_mode` *(a rival, added post-hoc, not the bar)* | 57.1% | 84.2% | 476 |
| `transition_table` **(the model)** | 52.9% | 84.7% | 476 |
| `global_mode` *(the bar)* | 43.1% | 83.8% | 476 |

- **Session-clustered, the pre-registered unit:** +0.0987 [+0.0202, +0.1725] (95%, sessions, n=124) — excludes zero
- Source-category clustered, D94's side-by-side: +0.0987 [-0.1845, +0.3018] (95%, subjects, n=13) — **includes zero**
- Rows, the optimistic bound: +0.0987 [+0.0588, +0.1387] (95%, rows, n=476) — excludes zero
- The table against `bounce_back` *(a rival)*: -0.0462 [-0.1570, +0.0612] (95%, sessions, n=124) — **includes zero**
- `bounce_back` against the bar: +0.1450 [+0.0857, +0.2013] (95%, sessions, n=124) — excludes zero
- The table against `constrained_mode` *(a rival, post-hoc)*:
  -0.0420 [-0.0990, +0.0050] (95%, sessions, n=124) — **includes zero**

**The bar spent 31.9% of its predictions on the source
category**, which a category *change* can never answer. The table did so on 0.0% of rows,
because a source's row holds no self-count. That much of the margin below is the floor
being handicapped rather than the table learning.

Pooled test rows span 124 sessions, median 2
rows each, largest 7%.

**With `unknown` answers kept** (905 labels), for
comparison and not for the verdict: the bar reaches
37.6% and the table
46.2%, session-clustered
+0.0866 [+0.0171, +0.1514] (95%, sessions, n=127) — excludes zero.

#### Folds

| Fold | Train | Test | `global_mode` | `transition_table` | Winner |
|---:|---:|---:|---:|---:|---|
| 0 | 316 | 95 | 32.6% | 45.3% | table |
| 1 | 411 | 95 | 44.2% | 55.8% | table |
| 2 | 506 | 95 | 41.1% | 57.9% | table |
| 3 | 601 | 95 | 47.4% | 33.7% | bar |
| 4 | 696 | 96 | 50.0% | 71.9% | table |

#### By source category

| From | Test rows | `global_mode` | `transition_table` | Most common answer |
|---|---:|---:|---:|---|
| `search` | 152 | 0.0% | 44.1% | `video` |
| `video` | 116 | 64.7% | 64.7% | `search` |
| `unknown` | 69 | 72.5% | 72.5% | `search` |
| `news` | 62 | 38.7% | 37.1% | `video` |
| `government` | 22 | 90.9% | 4.5% | `search` |
| `work` | 14 | — | — | *below the 20-row floor (D26)* |
| `property` | 13 | — | — | *below the 20-row floor (D26)* |
| `shopping` | 12 | — | — | *below the 20-row floor (D26)* |
| `dev` | 10 | — | — | *below the 20-row floor (D26)* |
| `learning` | 3 | — | — | *below the 20-row floor (D26)* |
| `ai` | 1 | — | — | *below the 20-row floor (D26)* |
| `finance` | 1 | — | — | *below the 20-row floor (D26)* |
| `social` | 1 | — | — | *below the 20-row floor (D26)* |

### `history-firefox`

915 events, 94 sessions, **248 category
changes** (192 within a session, 56
across a boundary). 37 session boundaries produced no label
because the category did not change. After D27, **202 labels** carry a
presentable answer; 46 answered `unknown`.

**Clears its bar.**

| Model | Top-1 | Top-3 | Rows |
|---|---:|---:|---:|
| `constrained_mode` *(a rival, added post-hoc, not the bar)* | 65.6% | 82.8% | 122 |
| `transition_table` **(the model)** | 63.1% | 82.8% | 122 |
| `bounce_back` *(a rival, not the bar)* | 53.3% | 81.1% | 122 |
| `global_mode` *(the bar)* | 50.0% | 81.1% | 122 |

- **Session-clustered, the pre-registered unit:** +0.1311 [+0.0526, +0.2065] (95%, sessions, n=46) — excludes zero
- Source-category clustered, D94's side-by-side: +0.1311 [-0.1091, +0.3978] (95%, subjects, n=11) — **includes zero**
- Rows, the optimistic bound: +0.1311 [+0.0656, +0.2049] (95%, rows, n=122) — excludes zero
- The table against `bounce_back` *(a rival)*: +0.0984 [+0.0286, +0.1650] (95%, sessions, n=46) — excludes zero
- `bounce_back` against the bar: +0.0328 [-0.0597, +0.1280] (95%, sessions, n=46) — **includes zero**
- The table against `constrained_mode` *(a rival, post-hoc)*:
  -0.0246 [-0.0556, +0.0000] (95%, sessions, n=46) — **includes zero**

**The bar spent 27.0% of its predictions on the source
category**, which a category *change* can never answer. The table did so on 0.0% of rows,
because a source's row holds no self-count. That much of the margin below is the floor
being handicapped rather than the table learning.

Pooled test rows span 46 sessions, median 2
rows each, largest 7%.

**With `unknown` answers kept** (248 labels), for
comparison and not for the verdict: the bar reaches
38.9% and the table
50.3%, session-clustered
+0.1141 [+0.0368, +0.1946] (95%, sessions, n=45) — excludes zero.

#### Folds

| Fold | Train | Test | `global_mode` | `transition_table` | Winner |
|---:|---:|---:|---:|---:|---|
| 0 | 80 | 24 | 50.0% | 66.7% | table |
| 1 | 104 | 24 | 45.8% | 58.3% | table |
| 2 | 128 | 24 | 58.3% | 66.7% | table |
| 3 | 152 | 24 | 45.8% | 54.2% | table |
| 4 | 176 | 26 | 50.0% | 69.2% | table |

#### By source category

| From | Test rows | `global_mode` | `transition_table` | Most common answer |
|---|---:|---:|---:|---|
| `unknown` | 36 | 55.6% | 55.6% | `search` |
| `search` | 33 | 0.0% | 57.6% | `ai` |
| `ai` | 30 | 90.0% | 90.0% | `search` |
| `dev` | 7 | — | — | *below the 20-row floor (D26)* |
| `government` | 5 | — | — | *below the 20-row floor (D26)* |
| `learning` | 3 | — | — | *below the 20-row floor (D26)* |
| `work` | 3 | — | — | *below the 20-row floor (D26)* |
| `wellness` | 2 | — | — | *below the 20-row floor (D26)* |
| `news` | 1 | — | — | *below the 20-row floor (D26)* |
| `shopping` | 1 | — | — | *below the 20-row floor (D26)* |
| `video` | 1 | — | — | *below the 20-row floor (D26)* |


## Limitations

- **A session is a declared 30-minute gap** (D17), and the cluster unit inherits that guess.
  Too long a timeout merges sittings into one cluster and widens the interval; too short
  splits one and narrows it. The `idle` permission (D96) is what would replace the guess
  with a measurement.
- **Cluster counts are not cluster quality.** Both are printed per corpus.
- **Categories are a fixed public map.** Roughly a fifth of visits land in
  `unknown`, by design — the map excludes employer, school, council and
  neighbourhood domains, because a domain list is a profile. D88 promoted shrinking that
  bucket, and D95 measured that session co-occurrence clustering does not reach it.
- **One person's browsing**, three browsers, never merged (D18). Agreement between corpora
  is weaker evidence than agreement between people would be.
- **The model reads only the source category.** No time of day, no domain, no run length,
  no recency. That is what the shipped table is; a richer multiclass model is a different
  target and is not registered.
- **No path or URL features.** The URL is dropped at the reader boundary.

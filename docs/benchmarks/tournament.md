# Tournament — what the in-browser constraint costs

> ### ⚠ Superseded — this report describes a retired target
>
> **`return_24h` was retired as the product target in D88.** Tise now predicts **`block_volume`**.
>
> Every number below was really measured and none of it has been withdrawn — it is kept deliberately, because it is the evidence that *justified* retiring the target. It is not a description of what Tise currently does, and no figure here should be quoted as a current result.
>
> See D88 in [`DECISIONS.md`](../../DECISIONS.md).

Generated by `tise_research.eval.tournament`. **Do not edit by hand.** Regenerate with:

```
uv run python -m tise_research.eval.tournament
```

Session timeout 1,800s (D17), horizon 24h, feature set `fs_3`.
Corpora measured **separately and never merged** (D18). XGBoost **3.4.1**,
`random_state=0`, `n_jobs=1`, `tree_method=exact`.

## Read this first

`logreg_fs3` is what the extension ships. The challengers are **research only** and
cannot ship at any score: the extension trains in a browser service worker with a
hand-written optimiser (D54) and one runtime dependency. So this page is **not a model
selection**. It measures what that constraint costs.

Both configurations, the seeds, the adoption rule and the choice of failure case were
fixed in **D84 before this code existed**, because the free parameter in a tournament is
not the score — it is how strong the opponent is made. Tuned, a challenger reports a
maximum over many draws that is mostly noise at these row counts; left at library defaults
on a few hundred rows it overfits and loses to an opponent that was quietly weakened.
**No hyperparameter search was run.**

**No gap is demonstrated.** On history-edge, the corpus D84 fixed the rule on, no challenger's subject-clustered interval excludes zero in its own favour. That is not a claim that the shipped model matches XGBoost — it is a claim that this much data cannot tell them apart, which is the same finding D80 reached about the model and its bar.

## The challengers

| Name | Configuration | Why this one |
|---|---|---|
| `xgb_small` | `max_depth=3`, `n_estimators=100`, `learning_rate=0.05`, `subsample=0.8`, `colsample_bytree=0.8`, `min_child_weight=5`, `reg_lambda=1.0` | depth-3 trees sized for a few hundred rows, per D84 |
| `xgb_default` | library defaults | library defaults, untouched, as a reviewer would first run it |

One difference from the shipped pipeline, registered in advance: the challengers see the
same eighteen design columns from the same `fs_3` rows, but nulls arrive
as `NaN` and XGBoost learns a missing direction for them rather than being mean-filled by
`prep.py`. Standardisation is omitted because tree splits are invariant to it. Imputation
is not neutral, so **any gap below includes whatever the better missing handling is
worth** — the null counts per corpus are printed so a reader can size that.

## Results

### history-chrome

322 labels, 141 pooled test rows, 5 folds.

| Model | Brier | Log loss | Skill vs base rate |
|---|---:|---:|---:|
| `category_base_rate` | 0.1963 | 0.5854 | +0.090 |
| `logreg_fs3` **(ships)** | 0.2024 | 0.6970 | +0.062 |
| `logreg_fs2` *(superseded by D83, here for the width ladder)* | 0.2096 | 0.7472 | +0.029 |
| `xgb_small` *(challenger, research only)* | 0.2141 | 0.6625 | +0.008 |
| `global_base_rate` | 0.2158 | 0.6254 | +0.000 |
| `time_of_day` | 0.2240 | 0.6427 | -0.038 |
| `xgb_default` *(challenger, research only)* | 0.2256 | 0.8449 | -0.046 |
| `majority_class` | 0.2908 | 4.0173 | -0.348 |
| `same_as_last` | 0.3195 | 1.0854 | -0.481 |

**The gap, as an interval.** Positive favours the challenger. This is the number D84 registered as *the* result; the point estimates above are the weaker reading.

| Challenger | Unit | Paired Brier difference (`logreg_fs3` − challenger) |
|---|---|---|
| `xgb_small` | categories | -0.0117 [-0.0306, +0.0468] (95%, subjects, n=12) — **includes zero** |
| `xgb_small` | rows | -0.0117 [-0.0366, +0.0137] (95%, rows, n=141) — **includes zero** |
| `xgb_default` | categories | -0.0232 [-0.0422, +0.0256] (95%, subjects, n=12) — **includes zero** |
| `xgb_default` | rows | -0.0232 [-0.0643, +0.0163] (95%, rows, n=141) — **includes zero** |

**The adoption rule does not fire here.** No challenger's subject-clustered interval excludes zero in its own favour against the shipped 0.2024, so no gap has been demonstrated on this corpus in either direction.

**Why these intervals are the width they are.** D82 found that an interval's width is a property of the comparison and not only of the sample. These three comparisons run over the *same* pooled rows in the same unit, and differ only in how much structure the two models share:

| Shipped model compared against | What they share | Point (positive favours the other model) | 95% width |
|---|---|---:|---:|
| `logreg_fs2` | 12 of 14 features (fs_2 vs fs_3) | -0.0072 | 0.0207 |
| `xgb_small` | all 18 input columns, different model class | -0.0117 | 0.0504 |
| `category_base_rate` | nothing but the category | +0.0060 | 0.0611 |

Same rows, same estimator, widths from **0.0207** to **0.0611** — a factor of 3.0 — ordered exactly by shared structure. Sample size explains none of it.

Nulls in the pooled rows — the challenger learns a missing direction for these and the shipped model mean-fills them, so part of any gap is that difference rather than the model class (D84):

| Nullable feature | Share of pooled rows null |
|---|---:|
| `priorReturnRate` | 1.4% |

### history-edge

503 labels, 271 pooled test rows, 5 folds.

| Model | Brier | Log loss | Skill vs base rate |
|---|---:|---:|---:|
| `xgb_small` *(challenger, research only)* | 0.1084 | 0.3714 | +0.431 |
| `logreg_fs2` *(superseded by D83, here for the width ladder)* | 0.1124 | 0.4015 | +0.410 |
| `logreg_fs3` **(ships)** | 0.1129 | 0.3911 | +0.407 |
| `category_base_rate` | 0.1254 | 0.4141 | +0.342 |
| `xgb_default` *(challenger, research only)* | 0.1287 | 0.5025 | +0.324 |
| `majority_class` | 0.1661 | 2.2941 | +0.128 |
| `time_of_day` | 0.1871 | 0.5632 | +0.017 |
| `global_base_rate` | 0.1904 | 0.5709 | +0.000 |
| `same_as_last` | 0.2625 | 0.9013 | -0.379 |

**The gap, as an interval.** Positive favours the challenger. This is the number D84 registered as *the* result; the point estimates above are the weaker reading.

| Challenger | Unit | Paired Brier difference (`logreg_fs3` − challenger) |
|---|---|---|
| `xgb_small` | categories | +0.0045 [-0.0082, +0.0362] (95%, subjects, n=12) — **includes zero** |
| `xgb_small` | rows | +0.0045 [-0.0086, +0.0187] (95%, rows, n=271) — **includes zero** |
| `xgb_default` | categories | -0.0157 [-0.0514, +0.0031] (95%, subjects, n=12) — **includes zero** |
| `xgb_default` | rows | -0.0157 [-0.0349, +0.0027] (95%, rows, n=271) — **includes zero** |

**The adoption rule does not fire here.** No challenger's subject-clustered interval excludes zero in its own favour against the shipped 0.1129, so no gap has been demonstrated on this corpus in either direction.

**Why these intervals are the width they are.** D82 found that an interval's width is a property of the comparison and not only of the sample. These three comparisons run over the *same* pooled rows in the same unit, and differ only in how much structure the two models share:

| Shipped model compared against | What they share | Point (positive favours the other model) | 95% width |
|---|---|---:|---:|
| `logreg_fs2` | 12 of 14 features (fs_2 vs fs_3) | +0.0006 | 0.0060 |
| `xgb_small` | all 18 input columns, different model class | +0.0045 | 0.0273 |
| `category_base_rate` | nothing but the category | -0.0124 | 0.0404 |

Same rows, same estimator, widths from **0.0060** to **0.0404** — a factor of 6.7 — ordered exactly by shared structure. Sample size explains none of it.

Nulls in the pooled rows — the challenger learns a missing direction for these and the shipped model mean-fills them, so part of any gap is that difference rather than the model class (D84):

| Nullable feature | Share of pooled rows null |
|---|---:|
| `priorReturnRate` | 0.4% |

### history-firefox

173 labels, 83 pooled test rows, 5 folds.

| Model | Brier | Log loss | Skill vs base rate |
|---|---:|---:|---:|
| `logreg_fs3` **(ships)** | 0.2131 | 0.6385 | +0.171 |
| `logreg_fs2` *(superseded by D83, here for the width ladder)* | 0.2219 | 0.6789 | +0.137 |
| `xgb_small` *(challenger, research only)* | 0.2260 | 0.6774 | +0.121 |
| `xgb_default` *(challenger, research only)* | 0.2437 | 0.9558 | +0.052 |
| `category_base_rate` | 0.2478 | 0.6983 | +0.036 |
| `global_base_rate` | 0.2571 | 0.7106 | +0.000 |
| `time_of_day` | 0.2575 | 0.7140 | -0.002 |
| `same_as_last` | 0.3478 | 1.1642 | -0.353 |
| `majority_class` | 0.4217 | 5.8258 | -0.640 |

**The gap, as an interval.** Positive favours the challenger. This is the number D84 registered as *the* result; the point estimates above are the weaker reading.

| Challenger | Unit | Paired Brier difference (`logreg_fs3` − challenger) |
|---|---|---|
| `xgb_small` | categories | -0.0129 [-0.1120, +0.0184] (95%, subjects, n=10) — **includes zero** |
| `xgb_small` | rows | -0.0129 [-0.0558, +0.0296] (95%, rows, n=83) — **includes zero** |
| `xgb_default` | categories | -0.0306 [-0.0937, +0.0085] (95%, subjects, n=10) — **includes zero** |
| `xgb_default` | rows | -0.0306 [-0.0888, +0.0263] (95%, rows, n=83) — **includes zero** |

**The adoption rule does not fire here.** No challenger's subject-clustered interval excludes zero in its own favour against the shipped 0.2131, so no gap has been demonstrated on this corpus in either direction.

**Why these intervals are the width they are.** D82 found that an interval's width is a property of the comparison and not only of the sample. These three comparisons run over the *same* pooled rows in the same unit, and differ only in how much structure the two models share:

| Shipped model compared against | What they share | Point (positive favours the other model) | 95% width |
|---|---|---:|---:|
| `logreg_fs2` | 12 of 14 features (fs_2 vs fs_3) | -0.0088 | 0.0208 |
| `xgb_small` | all 18 input columns, different model class | -0.0129 | 0.0854 |
| `category_base_rate` | nothing but the category | -0.0347 | 0.1122 |

Same rows, same estimator, widths from **0.0208** to **0.1122** — a factor of 5.4 — ordered exactly by shared structure. Sample size explains none of it.

Nulls in the pooled rows — the challenger learns a missing direction for these and the shipped model mean-fills them, so part of any gap is that difference rather than the model class (D84):

| Nullable feature | Share of pooled rows null |
|---|---:|
| `priorReturnRate` | 3.6% |

## The documented failure

### history-edge

The worst single row the shipped model produced on **history-edge**, chosen by largest squared error over every pooled test row (`unknown` excluded). It was selected by `max`, not by looking for an interesting one — D84 fixed that in advance, including the part about publishing a dull one.

- **Category:** `video`
- **Window ended:** 2026-08-08T15:42:28.606053+00:00
- **Tise said:** 99.1% chance of returning within 24 hours
- **What happened:** the person did not return
- **Squared error:** 0.9831 (the worst possible is 1.0000)

The feature vector it saw:

| Feature | Value |
|---|---:|
| `hoursSinceLastSeen` | 0.0708 |
| `firstSeenSaturation` | 0.9112 |
| `eventCount7d` | 373.0000 |
| `eventCount30d` | 1334.0000 |
| `daysSeen7d` | 7.0000 |
| `sessionCount7d` | 27.0000 |
| `categoryShare30d` | 0.5151 |
| `priorReturnRate` | 0.8926 |
| `priorSessionRate` | 0.6275 |
| `sessionEventCount` | 120.0000 |
| `sessionCategoryCount` | 2.0000 |
| `categoryEventsInSession` | 119.0000 |
| `hourOfDay` | 15.0000 |
| `dayOfWeek` | 5.0000 |

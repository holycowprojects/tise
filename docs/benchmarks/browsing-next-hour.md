# T-B · `browsing_next_hour` — will you be here?

Generated by `analysis/browsing_next_hour.py`. **Do not edit by hand.** Regenerate with:

```
uv run python analysis/browsing_next_hour.py
```

One label per clock hour in the observed span: **will at least one visit occur in the next
hour?** Feature set `pr_1`, **`history` compat class** — no dwell, no
permission, shippable the day it clears a bar.

## What was fixed before this ran, and what was fixed here

**D94 fixed the label, the subject, the cluster unit, the bar and the adoption rule**
before any of this existed, and git holds the order. It left the **feature set** open, so
`pr_1` was declared in `vector.py` and committed **unrun** ahead of these
numbers — the same discipline D81 and D99 used, for the same reason.

**The bar is the rhythm.** T-B's subject is the hour of day being predicted, which makes
`category_base_rate` the per-hour rate with no new baseline code — D94 chose the subject for
exactly that. A flat rate is reported and is explicitly **not** the bar: roughly a third of
these labels are hours a person is asleep, so beating a constant proves nothing.

**One rival was declared in advance:** `rhythm_7d`, this hour's trailing
7-day rate used directly as a probability. D92 and D102 both ended with
a simple rule matching or beating the fitted model, and in D102 the rule was only found
*after* the run, so it could not change anything. This one was written down first.

## Result

**`pr_1` clears the bar on `history-edge`.** The day-clustered interval against the per-hour rate excludes zero in the model's favour, which is the rule D94 fixed in advance. **D94's third prediction — that this target would not beat the rhythm — is wrong**, and the entry recording it named it as the one most likely to embarrass.

**And almost all of that margin comes from hours whose answer was already obvious.** 276 of the 1,290 pooled test rows follow an hour that already held a visit — the person is mid-session, and the per-hour rate has no way to know it. On the other 1,014, where *will you be here?* is a real question, the model **does not separate from the bar**: -0.0018 [-0.0052, +0.0016] (95%, days, n=55) — **includes zero**. So D94's prediction is wrong on the letter and right on the substance — circadian rhythm plus *are you here right now* is the whole signal, and the second half is the part that answers itself. The adoption rule was fixed before any of this was fitted and it fires; this paragraph is why the number is worth less than it looks.

- `logreg_pr1` has a lower Brier than the bar (`category_base_rate`) on: `history-chrome`, `history-edge`, `history-firefox`
- It **separates** from a flat rate (`global_base_rate`) on: `history-chrome`, `history-edge`
- It has a lower Brier than the two-line rule (`rhythm_7d`) on: `history-chrome`, `history-edge`, `history-firefox`
- It has a lower Brier than the post-hoc persistence rival (`previous_hour`) on:
  `history-chrome`, `history-edge`
- The bar itself beats a flat rate on: `history-chrome`, `history-edge`, `history-firefox` — this is the circadian
  rhythm's own effect size, and it is the reason the bar is what it is.

## `pr_1`

Eight features, every scale **declared and not fitted** (D81), everything unbounded
saturated. The set has to contain the rhythm — the bar *is* the rhythm, so a model denied
the hour of day could not beat it even in principle.

| Feature | What it is | Nullable |
|---|---|---|
| `hourSin` | the hour as a smooth cycle | no |
| `hourCos` | the other half of it | no |
| `isWeekend` | Saturday or Sunday | no |
| `minutesSinceLast` | since the last visit, saturated at 60 min | no |
| `visitsLastHour` | in the hour just ended, saturated at 5 | no |
| `visitsLastSixHours` | in the last six, saturated at 30 | no |
| `sameHourRate7d` | this clock hour's rate over the last 7 days | **yes** |
| `activeHourShare7d` | share of the last 168 hours with a visit | no |

The three places the model can beat the bar, and nowhere else: **the week** (`isWeekend` —
the bar pools every Tuesday 10am with every Sunday 10am), **right now**
(`minutesSinceLast`, `visitsLastHour`, `visitsLastSixHours` — whether the person is at the
machine, which a rhythm cannot hold), and **their own recent rhythm** (`sameHourRate7d`,
`activeHourShare7d` — a trailing version of the bar that moves when a routine does).

Only `sameHourRate7d` is nullable, and it is an absence of *corpus* rather than of
behaviour: in the first day of a corpus, none of the previous seven days was observed.
Filling it with zero would say the person was reliably absent then, which is the opposite
of unknown (D51). The visit counts are **measured zeros** — a quiet hour was counted and
found empty.

## Results

### history-chrome

5,129 events over a span of **1,341 labelled hours**
(56 days), base rate 13.0% —
that share of hours held at least one visit.

| Model | Brier | Log loss | Skill |
|---|---:|---:|---:|
| `logreg_pr1` **(the model)** | 0.1117 | 0.3667 | +0.189 |
| `previous_hour` *(post-hoc rival, not the bar)* | 0.1172 | 0.3943 | +0.149 |
| `time_of_day` | 0.1293 | 0.4286 | +0.061 |
| `category_base_rate` *(the bar — the per-hour rate)* | 0.1307 | 0.4329 | +0.051 |
| `global_base_rate` *(a flat rate, not the bar)* | 0.1377 | 0.4552 | +0.000 |
| `rhythm_7d` *(the declared two-line rule)* | 0.1482 | 0.8430 | -0.076 |
| `majority_class` | 0.1602 | 2.2139 | -0.163 |
| `same_as_last` | 0.2932 | 1.0023 | -1.129 |

#### The pre-registered comparison

`logreg_pr1` against `category_base_rate` — the per-hour rate — paired, **clustered by calendar
day**. Positive favours the model. On this corpus it **clears its bar**:

+0.0190 [+0.0063, +0.0341] (95%, days, n=35) — excludes zero

The model beat the bar on **5 of 5 folds**. A model with no skill at all does that or better 3.1% of the time, so the fold count is not evidence — it is printed because omitting it is how D60 read better than it was.

#### Against a flat rate, which is not the bar

+0.0261 [+0.0090, +0.0463] (95%, days, n=35) — excludes zero

D94 predicted this one is easy and said so before the run, which is why it is not the bar.

#### Against the declared two-line rule

`rhythm_7d` predicts this hour's trailing 7-day rate and nothing
else. `logreg_pr1` against it, day-clustered:

+0.0365 [+0.0200, +0.0541] (95%, days, n=35) — excludes zero

#### Isn't this just "were you browsing a minute ago"?

The question `minutesSinceLast` invites, and the one this page would be dishonest without.
`previous_hour` is a two-cell rate table: how often the next hour holds a visit given
the previous hour did, and given it did not. Same smoothing as the bar.
`logreg_pr1` against it, day-clustered:

+0.0055 [-0.0029, +0.0146] (95%, days, n=35) — **includes zero**

**Added after seeing the coefficients, labelled post-hoc, and it cannot change the verdict** —
D94 fixed the bar before anything was fitted, and D102 is the entry about exactly this.

#### Where the question is actually a question

An hour that follows a *busy* hour answers itself — the person is mid-session and the model says so. **129 of 805 pooled test rows are that case.** The other **676** follow an hour with no visit at all, and that is where *will you be here?* is a real question:

| On the 676 rows after a quiet hour | Brier |
|---|---:|
| `logreg_pr1` | 0.0856 |
| `category_base_rate` (the bar) | 0.0892 |

+0.0036 [+0.0010, +0.0061] (95%, days, n=35) — excludes zero

**Post-hoc, and a candidate rather than a finding.** Nothing registered this split in advance, so it is written down the way D104's *after search, where do you go* was: the question the numbers raise, for a future pre-registration to answer.

#### How the clusters are spread

The largest single day holds 3.0% of the 805 pooled test rows, median day 24. No cluster dominates the resampling, which is expected here: a day is 24 boundaries whether or not anyone browsed in them.

#### The same difference under two units

| Unit | Interval |
|---|---|
| day (the declared unit) | +0.0190 [+0.0063, +0.0341] (95%, days, n=35) — excludes zero |
| row (optimistic, never a claim) | +0.0190 [+0.0097, +0.0286] (95%, rows, n=805) — excludes zero |

#### Fitted weights

In-sample, standardised columns. For reading which features carry the model, not for
scoring it:

| Column | Standardised weight |
|---|---:|
| `hourCos` | -0.605 |
| `hourSin` | +0.544 |
| `minutesSinceLast` | -0.461 |
| `sameHourRate7d__missing` | -0.338 |
| `visitsLastHour` | +0.238 |
| `visitsLastSixHours` | +0.193 |
| `activeHourShare7d` | +0.161 |
| `sameHourRate7d` | -0.061 |
| `isWeekend` | -0.049 |

### history-edge

5,755 events over a span of **2,149 labelled hours**
(90 days), base rate 16.3% —
that share of hours held at least one visit.

| Model | Brier | Log loss | Skill |
|---|---:|---:|---:|
| `logreg_pr1` **(the model)** | 0.1079 | 0.3574 | +0.394 |
| `previous_hour` *(post-hoc rival, not the bar)* | 0.1188 | 0.4013 | +0.332 |
| `rhythm_7d` *(the declared two-line rule)* | 0.1525 | 0.7086 | +0.143 |
| `category_base_rate` *(the bar — the per-hour rate)* | 0.1625 | 0.4946 | +0.086 |
| `time_of_day` | 0.1672 | 0.5218 | +0.060 |
| `global_base_rate` *(a flat rate, not the bar)* | 0.1779 | 0.5597 | +0.000 |
| `majority_class` | 0.2147 | 2.9666 | -0.207 |
| `same_as_last` | 0.2278 | 0.7885 | -0.281 |

#### The pre-registered comparison

`logreg_pr1` against `category_base_rate` — the per-hour rate — paired, **clustered by calendar
day**. Positive favours the model. On this corpus it **clears its bar**:

+0.0546 [+0.0383, +0.0724] (95%, days, n=55) — excludes zero

The model beat the bar on **5 of 5 folds**. A model with no skill at all does that or better 3.1% of the time, so the fold count is not evidence — it is printed because omitting it is how D60 read better than it was.

#### Against a flat rate, which is not the bar

+0.0700 [+0.0505, +0.0911] (95%, days, n=55) — excludes zero

D94 predicted this one is easy and said so before the run, which is why it is not the bar.

#### Against the declared two-line rule

`rhythm_7d` predicts this hour's trailing 7-day rate and nothing
else. `logreg_pr1` against it, day-clustered:

+0.0446 [+0.0314, +0.0581] (95%, days, n=55) — excludes zero

#### Isn't this just "were you browsing a minute ago"?

The question `minutesSinceLast` invites, and the one this page would be dishonest without.
`previous_hour` is a two-cell rate table: how often the next hour holds a visit given
the previous hour did, and given it did not. Same smoothing as the bar.
`logreg_pr1` against it, day-clustered:

+0.0109 [+0.0057, +0.0167] (95%, days, n=55) — excludes zero

**Added after seeing the coefficients, labelled post-hoc, and it cannot change the verdict** —
D94 fixed the bar before anything was fitted, and D102 is the entry about exactly this.

#### Where the question is actually a question

An hour that follows a *busy* hour answers itself — the person is mid-session and the model says so. **276 of 1,290 pooled test rows are that case.** The other **1,014** follow an hour with no visit at all, and that is where *will you be here?* is a real question:

| On the 1,014 rows after a quiet hour | Brier |
|---|---:|
| `logreg_pr1` | 0.0829 |
| `category_base_rate` (the bar) | 0.0810 |

-0.0018 [-0.0052, +0.0016] (95%, days, n=55) — **includes zero**

**Post-hoc, and a candidate rather than a finding.** Nothing registered this split in advance, so it is written down the way D104's *after search, where do you go* was: the question the numbers raise, for a future pre-registration to answer.

#### How the clusters are spread

The largest single day holds 1.9% of the 1,290 pooled test rows, median day 24. No cluster dominates the resampling, which is expected here: a day is 24 boundaries whether or not anyone browsed in them.

#### The same difference under two units

| Unit | Interval |
|---|---|
| day (the declared unit) | +0.0546 [+0.0383, +0.0724] (95%, days, n=55) — excludes zero |
| row (optimistic, never a claim) | +0.0546 [+0.0412, +0.0680] (95%, rows, n=1290) — excludes zero |

#### Fitted weights

In-sample, standardised columns. For reading which features carry the model, not for
scoring it:

| Column | Standardised weight |
|---|---:|
| `minutesSinceLast` | -1.312 |
| `hourCos` | -0.414 |
| `hourSin` | +0.359 |
| `visitsLastSixHours` | -0.200 |
| `activeHourShare7d` | +0.194 |
| `sameHourRate7d__missing` | -0.157 |
| `sameHourRate7d` | +0.156 |
| `isWeekend` | -0.040 |
| `visitsLastHour` | +0.014 |

### history-firefox

915 events over a span of **914 labelled hours**
(38 days), base rate 11.2% —
that share of hours held at least one visit.

| Model | Brier | Log loss | Skill |
|---|---:|---:|---:|
| `previous_hour` *(post-hoc rival, not the bar)* | 0.0770 | 0.2850 | +0.060 |
| `logreg_pr1` **(the model)** | 0.0784 | 0.2703 | +0.043 |
| `category_base_rate` *(the bar — the per-hour rate)* | 0.0807 | 0.2805 | +0.016 |
| `time_of_day` | 0.0814 | 0.2940 | +0.007 |
| `global_base_rate` *(a flat rate, not the bar)* | 0.0819 | 0.3068 | +0.000 |
| `rhythm_7d` *(the declared two-line rule)* | 0.0846 | 0.5165 | -0.032 |
| `majority_class` | 0.0874 | 1.2079 | -0.067 |
| `same_as_last` | 0.1582 | 0.5608 | -0.931 |

#### The pre-registered comparison

`logreg_pr1` against `category_base_rate` — the per-hour rate — paired, **clustered by calendar
day**. Positive favours the model. On this corpus it **does not clear its bar**:

+0.0023 [-0.0034, +0.0078] (95%, days, n=24) — **includes zero**

Roughly **3,331 test rows** would be needed for a difference this size to exclude zero, against the 549 here — about 6x the data. That assumes the point estimate survives collecting it, which is exactly what is not known: if the true difference is zero, no sample size ever excludes it.

The model beat the bar on **4 of 5 folds**. A model with no skill at all does that or better 18.8% of the time, so the fold count is not evidence — it is printed because omitting it is how D60 read better than it was.

#### Against a flat rate, which is not the bar

+0.0036 [-0.0003, +0.0076] (95%, days, n=24) — **includes zero**

D94 predicted this one is easy and said so before the run, which is why it is not the bar.

#### Against the declared two-line rule

`rhythm_7d` predicts this hour's trailing 7-day rate and nothing
else. `logreg_pr1` against it, day-clustered:

+0.0062 [-0.0005, +0.0127] (95%, days, n=24) — **includes zero**

#### Isn't this just "were you browsing a minute ago"?

The question `minutesSinceLast` invites, and the one this page would be dishonest without.
`previous_hour` is a two-cell rate table: how often the next hour holds a visit given
the previous hour did, and given it did not. Same smoothing as the bar.
`logreg_pr1` against it, day-clustered:

-0.0014 [-0.0063, +0.0034] (95%, days, n=24) — **includes zero**

**Added after seeing the coefficients, labelled post-hoc, and it cannot change the verdict** —
D94 fixed the bar before anything was fitted, and D102 is the entry about exactly this.

#### Where the question is actually a question

An hour that follows a *busy* hour answers itself — the person is mid-session and the model says so. **48 of 549 pooled test rows are that case.** The other **501** follow an hour with no visit at all, and that is where *will you be here?* is a real question:

| On the 501 rows after a quiet hour | Brier |
|---|---:|
| `logreg_pr1` | 0.0638 |
| `category_base_rate` (the bar) | 0.0663 |

+0.0025 [-0.0036, +0.0083] (95%, days, n=24) — **includes zero**

**Post-hoc, and a candidate rather than a finding.** Nothing registered this split in advance, so it is written down the way D104's *after search, where do you go* was: the question the numbers raise, for a future pre-registration to answer.

#### How the clusters are spread

The largest single day holds 4.4% of the 549 pooled test rows, median day 24. No cluster dominates the resampling, which is expected here: a day is 24 boundaries whether or not anyone browsed in them.

#### The same difference under two units

| Unit | Interval |
|---|---|
| day (the declared unit) | +0.0023 [-0.0034, +0.0078] (95%, days, n=24) — **includes zero** |
| row (optimistic, never a claim) | +0.0023 [-0.0032, +0.0079] (95%, rows, n=549) — **includes zero** |

#### Fitted weights

In-sample, standardised columns. For reading which features carry the model, not for
scoring it:

| Column | Standardised weight |
|---|---:|
| `hourSin` | +0.613 |
| `hourCos` | -0.535 |
| `minutesSinceLast` | -0.461 |
| `sameHourRate7d` | +0.204 |
| `activeHourShare7d` | +0.106 |
| `isWeekend` | -0.100 |
| `visitsLastSixHours` | +0.056 |
| `visitsLastHour` | -0.046 |
| `sameHourRate7d__missing` | +0.039 |


## Limitations

- **One person's browsing**, three browsers, so agreement between corpora is much weaker
  evidence than three people agreeing would be.
- **A corpus is one browser.** An hour with no visits here may be an hour spent browsing
  somewhere else, which would enter as a false negative. D18 forbids merging histories, so
  this is not fixable by combining them — it is a property of the question.
- **An hour with the computer switched off is a real negative and reads as a choice.**
  Tise cannot tell "chose not to browse" from "was not at a machine", and for a prediction
  shown to a person those are the same answer.
- **Both boundary hours are excluded**, because both are positive by construction — they
  hold the first and last visit of the corpus by definition.
- **Retention shapes the span.** Chrome's history file holds nothing before a certain date
  (D64), so the span is what the browser kept, not what the person did.
- **In-sample weights.** The coefficient table is fitted on everything and is for reading
  which features carry the model, not for scoring it.

# Baselines — `return_24h`

> ### ⚠ Superseded — this report describes a retired target
>
> **`return_24h` was retired as the product target in D88.** Tise now predicts **`block_volume`**.
>
> Every number below was really measured and none of it has been withdrawn — it is kept deliberately, because it is the evidence that *justified* retiring the target. It is not a description of what Tise currently does, and no figure here should be quoted as a current result.
>
> See D88 in [`DECISIONS.md`](../../DECISIONS.md).

Generated by `tise_research.eval.report`. **Do not edit by hand.** Regenerate with:

```
uv run python -m tise_research.eval.backtest
```

Session timeout 1,800s (D17), horizon 24h, one label per
(category, session) (D16). Corpora are measured **separately and never merged** (D18).

## How to read this

These are baselines. None of them learns anything, and that is the point: on this data
the base rate is around 70%, so a model must beat *reporting the base rate* before it has
demonstrated anything at all.

- **Brier** — mean squared error of the probability. Lower is better. 0.25 is
  uninformative.
- **Log loss** — punishes confident mistakes far harder. ln(2) ≈ 0.693 is uninformative.
- **Skill** — fractional improvement over `global_base_rate`. 0 means no better than
  reporting the base rate. This is the honest headline.
- **Accuracy is deliberately absent.** With a 70% base rate, always answering "yes"
  scores 70% and has learned nothing.

Folds are **rolling-origin and expanding**: fold *n* trains on everything before its
cutoff and tests on the chunk immediately after. Nothing is shuffled and there is no
random seed — two runs produce identical output.

Every baseline is fitted on its training window and then **frozen**. `same_as_last`
therefore repeats the last outcome seen *in training* across the whole test window rather
than updating as it goes. That is weaker than an online version, and it is said here
rather than quietly improved.

## `full` versus `history`

Not applicable to this table, and that is a real result rather than an omission. None of
these baselines uses dwell time — they see only the category and `window_end` — so the
`full` and `history` variants are **identical by construction**. Every number here is
achievable by the shipped extension.

It stayed that way at T10. D35 stopped Tise measuring dwell in either direction, so every
feature in `fs_2` is compat class `history` and the `full` class is empty (D51). The
distinction is kept because it is real, not because anything currently uses it.

### history-chrome

322 labels over
2026-07-01 to 2026-08-26, base rate
**71.4%**, 5 folds.

| Model | Brier | Log loss | Skill vs base rate | Test labels |
|---|---:|---:|---:|---:|
| `category_base_rate` | 0.1963 | 0.5854 | 0.090 | 141 |
| `logreg_fs3` | 0.2024 | 0.6970 | 0.062 | 141 |
| `global_base_rate` | 0.2158 | 0.6254 | reference | 141 |
| `time_of_day` | 0.2240 | 0.6427 | -0.038 | 141 |
| `majority_class` * | 0.2908 | 4.0173 | -0.348 | 141 |
| `same_as_last` | 0.3195 | 1.0854 | -0.481 | 141 |

`*` majority-class is a hard 0/1 predictor. Its log loss is the clamp
(`LOG_LOSS_EPSILON`) doing the work, not a measurement — see `eval/metrics.py`.

**Per fold** (Brier, lower is better):

| Fold | Train | Test | Test ends | Base rate | `majority_class` | `global_base_rate` | `category_base_rate` | `same_as_last` | `time_of_day` | `logreg_fs3` |
|---:|---:|---:|---|---:|---:|---:|---:|---:|---:|---:|
| 0 | 128 | 38 | 2026-08-04 | 81.6% | 0.184 | 0.184 | 0.142 | 0.168 | 0.219 | 0.166 |
| 1 | 166 | 38 | 2026-08-07 | 94.7% | 0.053 | 0.124 | 0.087 | 0.003 | 0.132 | 0.055 |
| 2 | 204 | 38 | 2026-08-16 | 63.2% | 0.368 | 0.242 | 0.246 | 0.287 | 0.250 | 0.300 |
| 3 | 242 | 38 | 2026-08-19 | 86.8% | 0.132 | 0.139 | 0.121 | 0.476 | 0.142 | 0.101 |
| 4 | 280 | 42 | 2026-08-26 | 59.5% | 0.405 | 0.260 | 0.242 | 0.317 | 0.259 | 0.262 |

**Per category.** 4 of 13 categories cleared the floor of
20 test labels; the rest are counted but not scored (D26).

| Category | Test labels | Base rate | Best Brier | Best model |
|---|---:|---:|---:|---|
| search | 51 | 86.3% | 0.1212 | `logreg_fs3` |
| dev | 39 | 76.9% | 0.1871 | `category_base_rate` |
| ai | 22 | 72.7% | 0.2111 | `time_of_day` |
| social | 7 | 28.6% | **not modelled** | — |
| travel | 5 | 80.0% | **not modelled** | — |
| work | 5 | 0.0% | **not modelled** | — |
| news | 4 | 25.0% | **not modelled** | — |
| video | 3 | 66.7% | **not modelled** | — |
| learning | 2 | 50.0% | **not modelled** | — |
| finance | 1 | 0.0% | **not modelled** | — |
| government | 1 | 0.0% | **not modelled** | — |
| shopping | 1 | 0.0% | **not modelled** | — |

`unknown` holds **79** labels (92.5% positive) and is **excluded from every number above**.


### history-edge

503 labels over
2026-05-28 to 2026-08-26, base rate
**69.2%**, 5 folds.

| Model | Brier | Log loss | Skill vs base rate | Test labels |
|---|---:|---:|---:|---:|
| `logreg_fs3` | 0.1129 | 0.3911 | 0.407 | 271 |
| `category_base_rate` | 0.1254 | 0.4141 | 0.342 | 271 |
| `majority_class` * | 0.1661 | 2.2941 | 0.128 | 271 |
| `time_of_day` | 0.1871 | 0.5632 | 0.017 | 271 |
| `global_base_rate` | 0.1904 | 0.5709 | reference | 271 |
| `same_as_last` | 0.2625 | 0.9013 | -0.379 | 271 |

`*` majority-class is a hard 0/1 predictor. Its log loss is the clamp
(`LOG_LOSS_EPSILON`) doing the work, not a measurement — see `eval/metrics.py`.

**Per fold** (Brier, lower is better):

| Fold | Train | Test | Test ends | Base rate | `majority_class` | `global_base_rate` | `category_base_rate` | `same_as_last` | `time_of_day` | `logreg_fs3` |
|---:|---:|---:|---|---:|---:|---:|---:|---:|---:|---:|
| 0 | 201 | 60 | 2026-07-28 | 85.0% | 0.150 | 0.235 | 0.160 | 0.097 | 0.233 | 0.086 |
| 1 | 261 | 60 | 2026-08-03 | 81.7% | 0.183 | 0.198 | 0.139 | 0.138 | 0.192 | 0.098 |
| 2 | 321 | 60 | 2026-08-11 | 75.0% | 0.250 | 0.200 | 0.134 | 0.242 | 0.195 | 0.158 |
| 3 | 381 | 60 | 2026-08-18 | 83.3% | 0.167 | 0.170 | 0.104 | 0.752 | 0.173 | 0.168 |
| 4 | 441 | 62 | 2026-08-26 | 77.4% | 0.226 | 0.184 | 0.155 | 0.206 | 0.180 | 0.159 |

**Per category.** 4 of 13 categories cleared the floor of
20 test labels; the rest are counted but not scored (D26).

| Category | Test labels | Base rate | Best Brier | Best model |
|---|---:|---:|---:|---|
| video | 125 | 95.2% | 0.0478 | `logreg_fs3` |
| search | 71 | 87.3% | 0.1136 | `logreg_fs3` |
| news | 48 | 83.3% | 0.1667 | `majority_class` |
| dev | 10 | 20.0% | **not modelled** | — |
| government | 5 | 20.0% | **not modelled** | — |
| work | 5 | 20.0% | **not modelled** | — |
| learning | 2 | 50.0% | **not modelled** | — |
| ai | 1 | 0.0% | **not modelled** | — |
| finance | 1 | 0.0% | **not modelled** | — |
| property | 1 | 0.0% | **not modelled** | — |
| shopping | 1 | 0.0% | **not modelled** | — |
| social | 1 | 0.0% | **not modelled** | — |

`unknown` holds **56** labels (54.8% positive) and is **excluded from every number above**.


### history-firefox

173 labels over
2026-07-18 to 2026-08-25, base rate
**64.2%**, 5 folds.

| Model | Brier | Log loss | Skill vs base rate | Test labels |
|---|---:|---:|---:|---:|
| `logreg_fs3` | 0.2131 | 0.6385 | 0.171 | 83 |
| `category_base_rate` | 0.2478 | 0.6983 | 0.036 | 83 |
| `global_base_rate` | 0.2571 | 0.7106 | reference | 83 |
| `time_of_day` | 0.2575 | 0.7140 | -0.002 | 83 |
| `same_as_last` | 0.3478 | 1.1642 | -0.353 | 83 |
| `majority_class` * | 0.4217 | 5.8258 | -0.640 | 83 |

`*` majority-class is a hard 0/1 predictor. Its log loss is the clamp
(`LOG_LOSS_EPSILON`) doing the work, not a measurement — see `eval/metrics.py`.

**Per fold** (Brier, lower is better):

| Fold | Train | Test | Test ends | Base rate | `majority_class` | `global_base_rate` | `category_base_rate` | `same_as_last` | `time_of_day` | `logreg_fs3` |
|---:|---:|---:|---|---:|---:|---:|---:|---:|---:|---:|
| 0 | 69 | 20 | 2026-08-03 | 55.0% | 0.450 | 0.283 | 0.272 | 0.552 | 0.276 | 0.160 |
| 1 | 89 | 20 | 2026-08-06 | 70.0% | 0.300 | 0.210 | 0.180 | 0.166 | 0.198 | 0.194 |
| 2 | 109 | 20 | 2026-08-08 | 55.0% | 0.450 | 0.269 | 0.263 | 0.362 | 0.276 | 0.242 |
| 3 | 129 | 20 | 2026-08-20 | 50.0% | 0.500 | 0.280 | 0.296 | 0.452 | 0.261 | 0.280 |
| 4 | 149 | 24 | 2026-08-25 | 58.3% | 0.417 | 0.248 | 0.235 | 0.340 | 0.286 | 0.261 |

**Per category.** 3 of 11 categories cleared the floor of
20 test labels; the rest are counted but not scored (D26).

| Category | Test labels | Base rate | Best Brier | Best model |
|---|---:|---:|---:|---|
| ai | 35 | 68.6% | 0.2125 | `global_base_rate` |
| search | 31 | 71.0% | 0.1744 | `logreg_fs3` |
| dev | 6 | 16.7% | **not modelled** | — |
| government | 3 | 33.3% | **not modelled** | — |
| shopping | 2 | 0.0% | **not modelled** | — |
| work | 2 | 0.0% | **not modelled** | — |
| learning | 1 | 0.0% | **not modelled** | — |
| news | 1 | 0.0% | **not modelled** | — |
| video | 1 | 0.0% | **not modelled** | — |
| wellness | 1 | 0.0% | **not modelled** | — |

`unknown` holds **30** labels (57.1% positive) and is **excluded from every number above**.


## Limitations

- **One person's browsing.** Every number describes the author. Nothing generalises, and
  no claim here should be read as a claim about people in general.
- **Frozen baselines.** See above; an online `same_as_last` would score better.
- **Small tails.** Most categories never clear the label floor. That is a fact about the
  data, not a tuning choice, and it will not improve much with more history because the
  distribution is dominated by a handful of domains.
- **`logreg_fs2` is not a baseline.** It appears in these tables because D24 requires the
  baselines beside it on identical folds, and this is where those folds are defined. What
  it means — where it clears the D28 bar, where it does not, and the per-fold picture the
  pooled score hides — is in `model.md`, and that is the file to read about it.

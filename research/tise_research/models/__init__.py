"""Models, in ascending order of ambition.

Baselines first, and they are not a formality. On this data the base rate for
`return_24h` is around 70%, so a model has to beat *reporting that base rate* before it
has demonstrated anything. Every benchmark this project publishes reports the baselines
alongside the model, on identical folds, or it does not get published.
"""
